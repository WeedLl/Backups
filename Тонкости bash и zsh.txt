________________________________________________ Общее _____________________________________________
# О переносе строки в PS1 или PROMPT
    В zsh необходимо использовать конструкцию "$'\n'".
    В bash \n.

# О хоткеях в PS1/PROMPT
    В zsh хоткеи прописыватся через "%", в bash - "/".

# 'source $ZSH/oh-my-zsh.sh' выполняет инициализацию и compinit, так что некоторые изменения в переменных окружения
    PATH и FPATH необходимо делать до неё (для корректной работы некоторых плагинов. ?Можно проверить в этом ли
    проблема, если выполнить 'source ~/.zshrc' не перезагружая терминал.
